        while curr:
            if curr:
                allNodes.append(curr)
            curr = curr.next
        if n >= len(allNodes) or n <= 0:
        allNodes[-n-1].next = allNodes[-n].next
        curr = head
        allNodes = []
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
            return head.next
        return head
        
class Solution:
#         self.next = next
[

